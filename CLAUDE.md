# CLAUDE.md — 세션 상비 규칙 (얇게 유지할 것)
## 작업 시작 전 룰

작업을 시작하기 전에 95% 확신이 들 때까지 저에게 추가 질문을 해주세요.
확신이 안 서면 코드를 작성하지 마세요.

## 불확실한 정보 처리

정확히 모르는 내용은 추측으로 답하지 않는다. 모르거나 최신 정보가 필요한 경우 WebSearch, WebFetch 등의 도구로 직접 검색하여 정확한 정보를 찾아서 알려준다. 사용자에게 직접 찾아보라고 미루지 않는다.

## 언어 혼용 규칙

모든 응답은 한국어로 작성하되, 다음은 원문 그대로 인용한다:

- 코드 식별자(함수/변수/클래스명), 파일 경로, 셸 명령어
- 에러 메시지, 로그 출력, 라이브러리·API명

## 검증 의무 강화

"보통", "일반적으로", "아마", "대체로" 같은 모호한 표현으로 답하지 않는다. 이런 표현이 떠오르면 그 자체가 검색 신호다 — WebSearch나 공식 문서의 최신사항을 먼저 확인하고 단정적으로 답한다.

## 완료 보고 형식

코드 수정 후 "완료"라고 보고할 때는 다음 중 무엇을 실행했는지 명시한다:

- 빌드/타입 체크 결과 (예: `tsc 통과`)
- 테스트 결과 (예: `vitest 12개 통과`)
- 직접 실행/preview 확인 (예: `localhost:3000에서 동작 확인`)

실행하지 않았다면 "검증 미실시"라고 솔직히 적는다.

## DRY 원칙

유틸 함수, 헬퍼, 상수 등을 새로 작성하기 전 Grep으로 동일/유사 기능이 이미 있는지 먼저 확인한다. 있으면 재사용, 없으면 신규 작성. 의심스러우면 사용자에게 확인한다.

## 작업 범위 준수

요청받은 변경만 수행한다. 리팩토링, 무관한 코드 포맷팅 등은 하지 않는다. 필요하다고 판단되면 별도로 제안만 하고 사용자 승인을 받는다.

## 파괴적 명령 사전 확인

다음은 실행 전 반드시 사용자 확인을 받는다:

- git 강제 작업 (`reset --hard`, `push --force`, `branch -D`)
- 패키지 제거/다운그레이드, 마이그레이션 롤백
- 시스템 설정 변경 (환경변수, 레지스트리)


## 프로젝트 체제 (2026-07-21 대전환 — 3트랙)

**그레이박스 프로토타입 노선 폐기** (진단: 시각 피드백 루프 단절 — 눈 없는 Claude가 그리고 손 없는 유저가 봄 → 품질·열정 실패). 현재 체제:

- **Track 1 · 작은 게임 [최우선]** — Unity/C# 소품 게임을 **완성·출시**해 '완성의 기술'과 출시 파이프라인(페이지·빌드·피드백)을 경험한다. **사지방 = 기획·에셋 목록·태스크·C# 스크립트 사전 준비(Claude 주도) / 휴가(집) = 유저가 유니티로 순수 구현.** 스코프 규칙 엄수: 잼 사이즈("휴가 며칠 안에 코어 루프 완성 가능"). 첫 게임의 목표는 재미가 아니라 **출시 경험 그 자체**.
- **Track 2 · 본 게임** — 2D 메트로베니아(Steam, 전역 후 본격 개발). **소재 = 신전 · 세 짐승 신(여우·늑대·용) · 신앙 소실** `[2026-08-09 확정]`. **신앙이 식어 눈이 가려진 신들이 주인공의 몸에 시선을 얹어 동행하고**(★눈을 뜨는 건 가호를 켠 하나뿐), 그는 신들 대신 병든 땅을 보러 다닌다. 아트 톤 기조는 메이플 「시간의 신전」 리마스터(대리석·금세공·구름 하늘) — 유저가 AI 초벌+수정으로 전면 재작화 예정. 엔진은 **Unity**로 결정. (~~성창사~~ 명칭·컨셉은 보류)
- **[Track 2 장르 원칙 · 유저 확정]** 이 게임은 **메트로베니아**다 — 탐험·길 찾기·스토리·모험이 중심. **전투는 요소 중 하나이지 중심이 아니다**(메이플식 난사 전투 게임 아님). 설계가 전투로 기울면 되돌린다. 상세는 기획서 최상단 「장르 원칙」.
- **Track 3 · AI 활용 훈련** — 별도 시간 없음. Track 1·2 실전(브리프 주기·검증하기·역할 나누기)이 곧 훈련.

## ★ 문서 체계 (2026-08-09 일원화)

> **`docs/game-design-theory.md` = 기획 이론 기준선** (모그 강의 1~8강 학습 정본 · 2026-08-09).
> *어떻게* 기획하는가의 기준 — 기획의 정의 · 재미 3요소 · 시스템/콘텐츠 · **기획서 7단계** · **기획 의도(WHY)** ·
> §8 우리 프로젝트 진단(**현 문서는 컨셉 문서이지 기획서가 아니다**) · §9 작업 규율 7조. **설계 판단이 갈리면 여기로.**
>
> **`docs/game-design.md`가 기획의 유일한 정본이다** (v2 — 신전 노선 전면 개정). *무엇을* 만드는가.
> 세계 · 주인공 · 시스템 · 미결(§11) · 기각 이력(부록 A)이 전부 그 안에 있다. **기획 작업은 거기서 시작한다.**

- 구 「치환해서 읽기」 규칙은 **종료** — 문서가 신전 명사로 다시 쓰였다. 구 마법학교판·등뼈 판정 기록
  (`specs/2026-08-08-temple-route-spine.md`)은 **v2로 병합 후 삭제**(git 이력 커밋 `591f761` 참조).
- `archive/story-track.md` — 구 뼈대 「징표와 용사」. **인물·플롯은 폐기, 진행 방법론(§2.1·§2.5·§2.7·§2.8)은 이관 완료**
  `[08-09 · §11-10 해소]` ⇒ **방법론 참조처.** **§3 취향 프로필·기각 이력과 §3.5 서사 운영 원칙은 영구 유효** — 스토리 작업 전 필독.
- ★ **콘텐츠 3정본 체제** `[유저 지시 08-09 — 문서 일원화]`: **`game-design.md`(게임 디자인) · `story-design.md`(스토리 = 진행 골격+인물·사건) · `world-design.md`(월드 = 동선+신전 골격)**. 시스템 명세는 **`systems.md` 원장에 장(章)으로만 쌓는다.**
  ⚠ **specs/에 새 문서를 만들지 마라** — 파일 남발 방식은 종료. 새 내용은 위 정본에 장·절로 추가한다. (specs/ 잔여 = proto 맵 설계 등 소수)
- **`docs/agent-team.md` = 개발 방식 정본** `[08-09 확립]` — **누가** 하는가(팀 구성·반려권·권한). **아래 「작업 프로토콜」의 상세.**
- **`docs/loop-engineering.md` = 작업 실행 방식 정본** `[08-09]` — **어떻게 굴리는가**(자율 반복 루프의 GOAL·종료 조건·**토큰 예산 70% 규칙**). 현재 GDD 루프는 ⏸보류(A-10).
- **절대 수치의 정본은 `docs/unity/tuning.json`** — 기획서·proto가 여기서 유도한다.

## ★ 이 세션의 범위 `[유저 지시 2026-08-08]`

```
이 세션 = 기획 + 웹 테스트(proto/)        Unity 이식 = 완전히 별도 세션
```

**Unity 작업을 여기서 굴리지 않는다.** 인계 패키지(`docs/unity/`)는 완성돼 있고,
그걸 다른 세션에 넘기는 것으로 이 세션의 역할은 끝났다. 질문에 답할 수는 있으나
유니티 프로젝트를 만들거나 C# 을 쓰지 않는다. **Track 1(작은 게임)도 별도 세션이다.**

## 현재 진행 상태 (2026-08-08)

**Track 2는 구현 동결이 아니다.** 이동 검증용 웹 프로토타입이 완성돼 있다.

```
proto/index.html    신전 수련장 7층 · 단일 파일 · 의존성 0 · 검증 19/19 PASS
docs/unity/         Unity 이식 인계 패키지 (백지 AI가 그대로 만들 수 있는 명세)
art/                캐릭터 스프라이트시트 v3 (864×1924 · 13행)
```

배포: https://abback-go.github.io/yemogame2/proto/index.html
(`Enter` 플레이 · `C`/`V` 창대쉬 · `F` 이펙트 토글 · `T` 검증)

**설계 순서** `[유저 승인 08-09]`: **① 진행 골격 → ② 공간 골격 → ③ 일상 루프 → ④ 스토리 세부**
(①·①-c는 확정. 07-27의 「공간 먼저」 개정 — v2 전환으로 스토리가 가장 얇아졌다).
**다음 단계 = 시스템 기획서 ①「월드 그래프·게이팅·복귀」**(A-8 확인 5건 선행 — TODO 참조).
Unity 이식은 별도 세션 — `docs/unity/PROMPT.md`를 붙여넣고 ZIP을 주면 된다.

## 세션 시작 시 읽기 순서
`TODO.md`(현황·다음 액션) → **`docs/agent-team.md`(개발 방식 — 어떻게 일하는가)**
→ **`docs/game-design-theory.md`(기획 이론 — 어떻게 기획하는가)**
→ **`docs/game-design.md`(기획 정본 — 무엇을 만드는가)**
→ 스토리 작업 전 `archive/story-track.md` **§3·§3.5**(취향·기각 이력 — 영구 유효)
→ 프로토 작업 시 `docs/shadow-dagger-CLAUDE.md`(원칙 7조)·`docs/handoff-2026-08-08-session3.md`(검증 이력·오측 기록)
→ Unity 인계 시 `docs/unity/README.md`·`docs/unity/RENDERING.md`

## ★ 작업 프로토콜 — 에이전트 팀 체제 `[유저 승인 2026-08-09 · 항상 준수]`

> **상세 정본 = `docs/agent-team.md`. 에이전트 정의 = `.claude/agents/*.md`(5개).**
> 새 세션은 **작업을 시작하기 전에** 이 절을 읽고 그대로 따른다. 혼자 다 하지 않는다.

### 라우팅 — 무엇을 누구에게

| 작업 | 담당 |
|---|---|
| **시스템 기획서**(동작 방식·UI/UX·순서도·데이터 차트) | `system-designer` |
| **수식·곡선·수치**(데미지 공식·성장·약점 배율·자원 수급) | `balance-designer` |
| **웹 조사·레퍼런스·표절 검증** | `researcher` |
| **데이터 채우기**(퀘스트·적·방·항목) — *시스템 기획서가 있어야 착수* | `content-writer` |
| ★ **위 산출물의 검토** | **`design-critic` — 반려권 보유** |
| **기획 판단 · 컨셉 · 통합 · 유저와의 대화 · 최종 보고** | **advisor 직접**(위임 금지) |

- 여러 관점이 동시에 필요한 큰 결정은 **팀 논의**로 돌린다(선례 = `agent-team.md` §7). Workflow 사용은 **유저가 요청했을 때만.**
- **advisor가 직접 해야 하는 것**: 설계 판단 · 문서 통합 · 오너 보고 · **커밋**. 이론 §1.3(*"같이 일하는 사람 중 나보다 더 고민한 사람이 없어야 한다"*)은 위임할 수 없다.

### 불변 절차 4조

1. ★ **산출물은 오너에게 올라가기 전 `design-critic` 검토를 거친다.** **반려당한 초안을 advisor가 임의로 통과시킬 수 없다.** REJECT 사유는 셋뿐(이익 최대화로 게임이 망가짐 / 확정 전제와 충돌 / 해석의 여지로 구현 불가) — 그 외는 REVISE. **같은 사안 3회 반려 시 중단하고 쟁점을 오너에게 올린다.**
2. ★ **에이전트 보고를 믿지 말 것 — 직접 재검증 후 커밋.** 원문 대조·수치는 실제로 돌려 확인한다. (선례: 08-09에 팀이 보고한 C-1 실측을 advisor가 재현해 확인했다.)
3. ★ **기획 의도는 오너만 바꾼다.** advisor·에이전트는 **의도를 지킨 채 구현 방법만 절충**한다(이론 §1.6). 의도 자체가 문제로 보이면 **바꾸지 말고 쟁점으로 보고.**
4. **최종 승인은 항상 유저.** 절차: 대화 → 수정안 제시 → 유저 승인 → md 반영. **승인 전 파일 반영 금지.**

### 기타

- Track 1 코드: 유저 요청 시 **Unity C# 스크립트를 작성해 repo에 준비** → 유저가 휴가 때 조립. **엔진 실행 검증은 유저 몫** — 보고 시 "실행 검증 미실시" 정직 고지.
- **`[미정]`에는 「누가·언제·무엇을 기준으로」를 반드시 함께 적는다.** 그냥 `[미정]`은 해석의 여지다.
- ❌ **"해봐야 알죠" 금지**(이론 §1.4). 손맛 판정이 필요한 항목도 **무엇을·어떤 후보 범위에서** 판정할지까지는 설계해 둔다.

## 저장소 규칙
- 브랜치 `claude/magic-school-game-concept-wchpjn` 고정. PR #1이 추적 — **새 PR 생성 금지.**
- 커밋 트레일러: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_014hyj4wWQoEhcGguowFrsY6`. 커밋 전 `git config user.email noreply@anthropic.com`, `git config user.name Claude`. 모델 ID를 저장소 산출물에 넣지 않음.
- **`godot/` = 동결 아카이브 (수정 금지)** — 그레이박스 프로토타입의 검증 자산 참조용. **다른 캐릭터(불 마법사)라 현재 노선에 못 쓴다.** 검증된 지식의 정본은 docs/ 문서.
- 유저 빌드/파일 전달: ZIP 전송 대신 **GitHub 브랜치 Code → Download ZIP** (유저가 직접 최신을 받음). 유저 커밋은 GitHub 웹 업로드 또는 github.dev(`.` 키).
- `gh-pages` = 배포 전용 유지. **`proto/index.html`이 현재 주력 배포물.** (구 `prototype/index.html`·`map.html` 미러도 유지). 월드 데이터: `archive/world/regions/*.json` 원본 → `python3 tools/worldmap_build.py` (산출물 직접 수정 금지).

## 프로토타입 작업 규칙 (`proto/`)

상세는 `docs/shadow-dagger-CLAUDE.md`의 개발 원칙 7조. 특히:

- **단일 파일 `file://` 실행을 깨지 마라.** 외부 `fetch` 금지 (사지방 PC에서 브라우저만으로 열어야 한다).
- **`PHYS` 값을 바꾸지 마라.** 도달 수치가 맵 게이팅 전체를 결정한다. 하나만 바꿔도 19항목이 무효가 된다.
- **검증에 하드코딩 리터럴 금지.** 데이터·`PHYS`에서 유도할 것. (어겨서 시퀀스 브레이크 5건이 「12/12 통과」로 새어나간 전례)
- **역검증 필수.** 일부러 망가뜨려 FAIL이 재현되기 전에는 「통과」라고 보고하지 마라.
- 변경 후 항상 `node proto/verify-map.mjs` **19/19** + `FX.enabled` on/off 양쪽 확인.
  (이 컨테이너에서는 `node_modules/playwright` 심볼릭 링크가 필요하다.)
- **검증 하네스 자체도 틀린다.** 이번 세션 실제 사례가 `handoff-...session3.md` §5에 있다 —
  advisor가 3번, worker가 2번 잘못 측정했다. 극단값을 주입해 수치가 반응하는지 먼저 확인할 것.

## 유저 컨텍스트
군 복무 중(사지방: 브라우저 실질 사용, 전역 ~2027-02). **유니티 경험자** — 휴가 때 집에서 유니티로 구현. 대화는 한국어. 토큰 사용량에 민감 — 오케스트레이션은 얇게. **열정 관리가 최우선 자원** (그레이박스 소진 전례 — 유저가 볼 화면의 질과 완성 경험이 연료).
