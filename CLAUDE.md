# CLAUDE.md — 세션 상비 규칙 (얇게 유지할 것)
## 작업 시작 전 룰

작업을 시작하기 전에 95% 확신이 들 때까지 저에게 추가 질문을 해주세요.
확신이 안 서면 코드를 작성하지 마세요.

## 불확실한 정보 처리

정확히 모르는 내용은 추측으로 답하지 않는다. 모르거나 최신 정보가 필요한 경우 WebSearch, WebFetch 등의 도구로 직접 검색하여 정확한 정보를 찾아서 알려준다. 사용자에게 직접 찾아보라고 미루지 않는다.

## 언어 혼용 규칙

모든 응답은 한국어로 작성하되, 다음은 원문 그대로 인용한다:

- 코드 식별자(함수/변수/클래스명), 파일 경로, 셸 명령어
- 에러 메시지, 로그 출력, 라이브러리·API명

## 검증 의무 강화

"보통", "일반적으로", "아마", "대체로" 같은 모호한 표현으로 답하지 않는다. 이런 표현이 떠오르면 그 자체가 검색 신호다 — WebSearch나 공식 문서의 최신사항을 먼저 확인하고 단정적으로 답한다.

## 완료 보고 형식

코드 수정 후 "완료"라고 보고할 때는 다음 중 무엇을 실행했는지 명시한다:

- 빌드/타입 체크 결과 (예: `tsc 통과`)
- 테스트 결과 (예: `vitest 12개 통과`)
- 직접 실행/preview 확인 (예: `localhost:3000에서 동작 확인`)

실행하지 않았다면 "검증 미실시"라고 솔직히 적는다.

## DRY 원칙

유틸 함수, 헬퍼, 상수 등을 새로 작성하기 전 Grep으로 동일/유사 기능이 이미 있는지 먼저 확인한다. 있으면 재사용, 없으면 신규 작성. 의심스러우면 사용자에게 확인한다.

## 작업 범위 준수

요청받은 변경만 수행한다. 리팩토링, 무관한 코드 포맷팅 등은 하지 않는다. 필요하다고 판단되면 별도로 제안만 하고 사용자 승인을 받는다.

## 파괴적 명령 사전 확인

다음은 실행 전 반드시 사용자 확인을 받는다:

- git 강제 작업 (`reset --hard`, `push --force`, `branch -D`)
- 패키지 제거/다운그레이드, 마이그레이션 롤백
- 시스템 설정 변경 (환경변수, 레지스트리)


## 프로젝트 체제 (2026-07-21 대전환 — 3트랙)

**그레이박스 프로토타입 노선 폐기** (진단: 시각 피드백 루프 단절 — 눈 없는 Claude가 그리고 손 없는 유저가 봄 → 품질·열정 실패). 현재 체제:

- **Track 1 · 작은 게임 [최우선]** — Unity/C# 소품 게임을 **완성·출시**해 '완성의 기술'과 출시 파이프라인(페이지·빌드·피드백)을 경험한다. **사지방 = 기획·에셋 목록·태스크·C# 스크립트 사전 준비(Claude 주도) / 휴가(집) = 유저가 유니티로 순수 구현.** 스코프 규칙 엄수: 잼 사이즈("휴가 며칠 안에 코어 루프 완성 가능"). 첫 게임의 목표는 재미가 아니라 **출시 경험 그 자체**.
- **Track 2 · 본 게임 기획 숙성** — 2D 마법학교 메트로베니아(Steam, 전역 후 본격 개발). **구현 동결, 기획만 진행**: 스토리 미정 결정 → 기획서 대통합(새 스토리 반영) → 월드 재정합 → 데모 30분 설계. 엔진은 착수 시점에 재결정.
- **Track 3 · AI 활용 훈련** — 별도 시간 없음. Track 1·2 실전(브리프 주기·검증하기·역할 나누기)이 곧 훈련.

## 세션 시작 시 읽기 순서
`TODO.md`(현황·최근 개정 로그) → 작은 게임 작업 시 `docs/small-game/`(생성 시) → 본 게임 기획 시 `docs/game-design.md`·`docs/story-track.md`·`docs/world-map.md`

## 작업 프로토콜 (유저 지시 — 항상 준수)
- 나(메인 세션) = **advisor**: 기획·브리프·검증·멘토링(유니티/개발 질문 즉답). 설계 문서(md)는 advisor 직접 작성. 대량 리서치·기계적 노동은 opus worker 위임 — **worker 보고를 믿지 말 것**(직접 재검증 후 커밋).
- Track 1 코드: 유저 요청 시 **Unity C# 스크립트를 작성해 repo에 준비** → 유저가 휴가 때 유니티에서 조립. **엔진 실행 검증은 유저 몫** — 보고 시 "실행 검증 미실시" 정직 고지.
- **최종 승인은 항상 유저.** 설계 변경 절차: 대화 → 수정안 제시 → 유저 승인 → md 반영 → 다음 단계. 승인 전 파일 반영 금지.

## 저장소 규칙
- 브랜치 `claude/magic-school-game-concept-wchpjn` 고정. PR #1이 추적 — **새 PR 생성 금지.**
- 커밋 트레일러: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_014hyj4wWQoEhcGguowFrsY6`. 커밋 전 `git config user.email noreply@anthropic.com`, `git config user.name Claude`. 모델 ID를 저장소 산출물에 넣지 않음.
- **`godot/` = 동결 아카이브 (수정 금지)** — 그레이박스 프로토타입의 검증 자산(이동·전투 수치, 시스템 구조) 참조용. 검증된 지식의 정본은 docs/ 문서.
- 유저 빌드/파일 전달: ZIP 전송 대신 **GitHub 브랜치 Code → Download ZIP** (유저가 직접 최신을 받음). 유저 커밋은 GitHub 웹 업로드 또는 github.dev(`.` 키).
- `gh-pages` = 배포 전용 유지 (prototype/index.html·map.html 미러). 월드 데이터(Track 2): `docs/world/regions/*.json` 원본 → `python3 tools/worldmap_build.py` (산출물 직접 수정 금지).

## 유저 컨텍스트
군 복무 중(사지방: 브라우저 실질 사용, 전역 ~2027-02). **유니티 경험자** — 휴가 때 집에서 유니티로 구현. 대화는 한국어. 토큰 사용량에 민감 — 오케스트레이션은 얇게. **열정 관리가 최우선 자원** (그레이박스 소진 전례 — 유저가 볼 화면의 질과 완성 경험이 연료).
